# Desviaciones manual ↔ realidad (sensor_sunat)

Registro exigido por la spec VIGÍA: sección, esperado, obtenido, fecha.

| Fecha | Sección spec/manual | Esperado | Obtenido / decisión |
|---|---|---|---|
| 2026-08-04 | Spec §5 tabla de endpoints | Cumplimiento RVIE = `[EXTRAER Ventas §5.18]`, tipo TICKET | En el manual v30 real, cumplimiento es **§5.34** y NO usa ticket: responde directo `{archivoPdf: base64, nombreArchivoPdf}`. Propuesta es §5.18. La numeración de la spec estaba desplazada; se usó el TOC real de ambos manuales. |
| 2026-08-04 | Ventas §5.16 / Compras §5.31 | Estados de ticket según Anexo III | Anexo III (Ventas p.80): 01 Cargado, 02 Validando, 03 Procesado con Errores, 04 Procesado sin errores, 05 En proceso, 06 Terminado. §5.17 dice descargable cuando `codProceso` es "3 o 4"; se tratan 03/04/06 como terminales. |
| 2026-08-04 | Spec §1 (`pip install httpx`) | httpx | El proyecto ya usa `requests` en todos los módulos; se usa `requests` para no añadir dependencia. Sin cambios funcionales. |
| 2026-08-04 | Ventas §5.1 token | `username: {RUC} {USUARIO}` (el manual muestra un espacio) | El cliente prueba primero `RUC USUARIO` (con espacio, literal del manual) y si el token endpoint lo rechaza reintenta `RUCUSUARIO` concatenado. Confirmado 2026-08-04 con el smoke real: **con espacio SUNAT devuelve `access_denied`; el formato aceptado es el concatenado `RUCUSUARIO`** (el cliente lo intenta en ese orden y funciona). |
| 2026-08-04 | Hito P0 | Token + periodos RVIE | El token se emite correctamente y su `aud` incluye `api-sire … /v1/contribuyente/migeigv` (indicador 1), pero **todo endpoint SIRE responde `401 Authorization Required` (HTML de nginx)**, con curl y con requests, HTTP/1.1 y HTTP/2, ambos libros. El `gt` del recurso es `001000`, mientras los tokens emitidos por el portal web traen `100100`. Acción pendiente del usuario en SOL. |
| 2026-08-05 | Hito P0 (2º token) | Diagnóstico del 401 | Re-probado con un token nuevo emitido por el usuario: sigue 401. Aislado: es un **401 crudo de nginx (`Server: nginx`, HTML), no un JSON de la app** → lo rechaza el gateway/WAF ANTES de llegar al servicio SIRE. Igual en `/periodos`, con slash final, y en la raíz `/migeigv`; mismo resultado en RVIE y RCE. El token es estructuralmente válido, vigente y su `aud` incluye `migeigv` indicador 1. **Conclusión: el código y la colección Postman están correctos; el bloqueo es de autorización del lado de SUNAT.** Causa más probable (por orden): (1) el **usuario secundario DENEYELA** con el que se generaron las credenciales y el token **no tiene asignados los menús/perfil RVIE-RCE (SIRE)** en "Administración de usuarios secundarios"; (2) propagación de credenciales recién creadas (esperar hasta ~1 h / 24 h); (3) la app se registró sin marcar/guardar la URI "MIGE RCE y RVIE - SIRE". Verificar (1) primero: entrar a SIRE por el portal web con ese mismo usuario — si el portal tampoco deja, es permiso de usuario, no de API. |
| 2026-08-04 | Compras §5.14 FV0621 | Spec la lista como `GET` | El manual v22 documenta **método PUT** aunque el servicio "permite consultar". Se implementa como PUT (sin body) y se permite explícitamente en la lista blanca por ser consulta documentada. |
| 2026-08-04 | Spec §3 layout TXT "en los anexos" | Layout posicional de la propuesta en anexos | Los anexos del manual v30 (I–VI) no traen layout de columnas del TXT; el TXT de la propuesta trae **fila de cabecera con nombres de columna separados por barra vertical (pipe)**. El parser mapea por nombre de cabecera y guarda columnas no mapeadas en `raw_extra`. |
| 2026-08-04 | Ventas §5.23 casillas | `tipoDescarga`: 0 txt / 1 xls / 2 pdf (tabla de parámetros) | La evidencia del propio manual usa `.../reporte/2/txt` (string). Se usa el código numérico `0` primero; si SUNAT lo rechaza, probar el literal `txt` y anotar aquí. |
